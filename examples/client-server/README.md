# About this example 
This test simulates an application which is communicating with multiple API servers asynchronously.

client.c is the application.
echoserver.c is the async echoserver

to run this application, execute the echoserver in one terminal, then client in another terminal.
